{
  "name": "scrum-poker",
  "private": true,
  "packageManager": "pnpm@10.15.0",
  "engines": {
    "node": ">=20 <21"
  },
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "typecheck": "pnpm -r typecheck",
    "lint:no-sockets": "node -e \"process.exit(0)\"",
    "test:e2e": "node -e \"process.exit(0)\"",
    "test:load": "node -e \"process.exit(0)\""
  },
  "devDependencies": {
    "@types/node": "^22.18.0",
    "cross-env": "^10.0.0",
    "eslint": "^9.35.0",
    "prettier": "^3.6.2",
    "start-server-and-test": "^2.1.0",
    "typescript": "^5.9.2",
    "vitest": "^3.2.4"
  }
}
